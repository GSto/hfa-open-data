app.factory('DistrictMap', ['$http', function($http) {
	var DistrictMap = {};
	/*
	$http.jsonp('url', {})
		.success(function(data, status, headers, config) {

		})
		.error(function(data, status, headers, config) {
			
		});
	*/
	return DistrictMap;
}]);
